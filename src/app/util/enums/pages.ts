export const enum Pages {
  MAIN = 'main',
  LOGIN = 'login',
  REGISTRATION = 'registration',
  NOT_FOUND = 'not-found',
  CATALOG = 'catalog',
  CAT_DETAILS = 'cat-details',
  PROFILE = 'profile',
  BASKET = 'basket',
}

export const ID_SELECTOR = '{id}';
